<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Sigma3.Views.Home"
             xmlns:forms ="clr-namespace:Microcharts.Forms;assembly=Microcharts.Forms"
             >
    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="search.png" Text="Sigma" ></ToolbarItem>

    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout>
            <StackLayout>
                <Label Text="Sigma" FontSize="25"  TextColor="White" TranslationY="-40"  HorizontalOptions="Center" ></Label>
            </StackLayout>
            <ListView x:Name="topMoversListView" ItemSelected="StockSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text ="{Binding topMovers} " Detail="{Binding combine}" TextColor="black" DetailColor="#2196F3" ></TextCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <ImageButton Source="sigma.png"
                    HorizontalOptions="Center"
                    VerticalOptions="CenterAndExpand" />
            
            
        </StackLayout>
    </ContentPage.Content>
    

</ContentPage>